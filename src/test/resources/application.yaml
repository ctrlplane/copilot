logging:
  level:
    org:
      springframework:
        web:
          reactive: DEBUG
          client: DEBUG
spring:
  file:
    enabled: true
    path: "sample/kek-test.pem"
  cloud:
    vault:
      enabled: false